[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "atrox3d"
version ="1.0.0"

# [project.scripts]
# snakey = "snakesay.__main__:main"
